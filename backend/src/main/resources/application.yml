spring:
  profiles:
    include: secret

  datasource:
    url: jdbc:mysql://localhost:3306/moya_list?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: moya
    password: moya1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

server:
  port: 8080

app:
  frontend-url: http://localhost:5173
